import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Gap = ({height, wedth}) => {
  return (
    <View style={{height:height, width:wedth}}/>

  )
}

export default Gap;

const styles = StyleSheet.create({})