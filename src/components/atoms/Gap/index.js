import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const Gap = () => {
  return (
    <View>
      <Text>Gap</Text>
    </View>
  )
};

export default Gap;

const styles = StyleSheet.create({});